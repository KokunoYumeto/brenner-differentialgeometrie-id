# Independent reader and mathematics QA — Unit 16

Status: **pass**  
Date: 2026-08-25  
Scope: Lecture 16, Worksheet 16, and the exact source-supplied Solutions 1 and 12.

## Reader review

The Indonesian unit was compared directly with all four frozen expanded German surfaces. The final reader uses natural field language for Riemannian manifolds and metrics, fundamental matrices, induced metrics, the vector-field/one-form correspondence, canonical volume forms and measures, parametrized submanifolds, length, and Riemannian vector bundles. `Sphäre` is rendered as `sfer`, continuous differentiability follows the admitted natural phrase, and the sentence introducing the graph diffeomorphism is complete.

No reader-facing German remains outside deliberately preserved source locators, semantic identifiers, and category identities. The lecture keeps all three sections, four fact/proof blocks, three examples, two media surfaces and licenses, 780 TeX commands, 74 inline-math spans, and every formula in source order. The worksheet retains all 21 exercises and the two exact solution markers.

## Mathematical review

The independent audit confirmed three necessary mathematical repairs:

1. For an `m`-dimensional closed submanifold of an `n`-dimensional ambient manifold, the adapted chart's zero factor is `R^{n-m}`, not `R^{m-n}`.
2. The positive-square-root pullback formula for the canonical volume form requires the parametrizing diffeomorphism to preserve orientation; otherwise a minus sign can occur.
3. The regular-curve example needs the image orientation induced by the parametrization before its signless formula is valid.

The exact Commons metadata independently identifies the creator of `Sphere with three handles.png` as Oleg Alexandrov; the source spelling `Alexandrow` is not propagated. The German typo `ererbt` is recorded but its intended meaning is translated normally.

Both supplied solutions were recomputed. Solution 1 gives the induced coefficient `g(t)=1+4t^2`. Solution 12 correctly obtains the tangent vectors, Gram entries, and area factors `sqrt(1+5u^2)` and `sqrt(1+v^2+9v^4)`. Its two protected reader-visible conjunctions are translated to `dan` with the vector formulas unchanged. No worksheet-level mathematical defect was found.

The cumulative build then exposed one translation-only blank paragraph inside a comparison-chain argument expanded in math mode. Removing that paragraph changes no mathematical token or reader sentence and restores the intended TeX structure; the exact final target, translation receipt, prepared fragment, and preparation receipt are bound under `O011-TRANS-0192`.

## Closure

- 21 exercises in exact source order: 16 practice and five graded.
- Graded point values: `4, 3, 4, 6, 6`, totaling 23.
- All 21 source hint fields remain blank.
- Exactly two supplied solutions: 1 and 12.
- No solution is invented for the remaining nineteen exercises.
- Exactly two public-domain media assets, with canonical byte and rights identities preserved.
- Four translation receipts and four preparation receipts pass.
- Ten explicit correction/translation records bind every target departure and build-structure repair.

No cumulative PDF, HTML, backend, publication, or public-readback gate is claimed at this per-unit boundary.
