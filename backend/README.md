# O011 modular backend

This backend is additive to the Indonesian reader. It follows the coordinator's interoperability envelope without making the reader depend on a database or proprietary service.

- `schema/o011-record-v1.schema.json` defines the common record envelope.
- `records.jsonl` is the canonical deterministic UTF-8/LF stream for Units 1–10.
- `records.csv` is a deliberately lossy exchange projection of common fields.
- `MANIFEST.json` binds the generated views, source/target inputs, and generator.
- `unit01_records_frozen.jsonl` is the immutable 174-record Unit 1 baseline. The full 1,363-record Units 1–7 prefix is preserved byte-for-byte in every Units 8–10 export.

Stable IDs use source order and source-local identity, never translated titles or rendered page numbers. Display identifiers retain Brenner's source numbering (`1.x` through `10.x`) rather than the standalone book wrapper's chapter prefixes. Corrections and component rights remain separate records. Regenerate the combined backend only through `scripts/export_backend_v10.py`; generated files must not be hand-edited.

Unit 1 exposes one PDF `artifact` and receipt-backed `qa_event` records for structural translation checks, exact media closure, two-cycle reproducible PDF build, structural/accessibility inspection, and all-page visual inspection. Evidence paths are repository-relative; receipt hashes bind the records without copying machine-local or temporary-render paths into the public views.

Unit 2 adds the lecture/worksheet pair, three lecture segments, nineteen exercises, five supplied solutions, four concepts, two Commons assets with separate rights records, seven translated-TeX artifacts, and the sixteen documented corrections `O011-CORR-0012`–`0027`.

Unit 3 adds its lecture/worksheet pair, two lecture segments, twenty-one exercises, exactly two supplied solutions, five concepts, twenty admitted term records, three Commons assets with separate rights records, five artifacts, eleven QA events, and the ten documented corrections `O011-CORR-0028`–`0037`. It binds the cumulative through-Unit-3 PDF, exact media closure, two-cycle reproducible build, structural/accessibility inspection, all-page visual inspection, and independent mathematical audit. The cumulative backend contains 591 records; the validation receipt is `qa/unit-03/backend_qa.json`. No HTML artifact is claimed at this boundary.

Unit 4 adds one lecture segment, fifteen exercises, exactly the two source-supplied solutions for Exercises 7 and 10, five concepts, eighteen admitted term records, six artifacts, nineteen QA events, and the eight documented corrections `O011-CORR-0038`–`0045`. Its mathematical-media closure is exactly empty. The lone official Commons PDF is represented only as a local historical numbering/order witness with an explicit non-release rights record because its internal CC BY-SA 3.0 declaration and Commons CC BY-SA 4.0 metadata disagree. Unit 4 adds 222 records, bringing the cumulative backend to 813; the validation receipt is `qa/unit-04/backend_qa.json`. No HTML artifact is claimed at this boundary.

Unit 5 adds the lecture/worksheet pair, three lecture segments, fifteen exercises, exactly one source hint at Exercise 13, exactly one source-supplied solution at Exercise 1, eight concepts, fifteen admitted terms, one Commons SVG asset with its CC BY-SA 3.0 rights record, five artifacts, fifteen QA events, eighty relations, and the eight documented corrections `O011-CORR-0046`–`0053`. Multi-surface corrections carry every target binding and every applicable correction manifest. Unit 5 adds 156 records, bringing the cumulative backend to 969; the validation receipt is `qa/unit-05/backend_qa.json`. The 813-record JSONL and 814-line CSV baseline is byte-identical. No HTML artifact is claimed at this boundary.

Unit 6 adds the lecture/worksheet pair, three lecture segments, eighteen exercises, and exactly the three source-supplied solutions for Exercises 2, 6, and 9. All hint fields are blank, and the other fifteen exercises are recorded explicitly as having no source solution rather than receiving invented solution records. It also adds eight concepts, twenty-four admitted terms (`O011-TERM-0111`–`0134`), the Commons SVG `Parallel transport sphere2.svg` with creator and CC BY-SA 3.0 rights, eight translated/evidence artifacts, fourteen QA events, 105 relations, and all sixteen corrections `O011-CORR-0054`–`0069`. The terminology field-source audit, its bounded idempotent propagation receipt, and the exact model provenance `OpenAI Codex gpt-5.6-sol, Ultra` are first-class evidence surfaces. Unit 6 adds 204 records, bringing the cumulative backend to 1,173; the validation receipt is `qa/unit-06/backend.json`. The 969-record JSONL and 970-line CSV baseline is byte-identical. The manifest binds the final 105-page cumulative Unit 6 PDF and its deterministic build, structural, and all-page visual receipts without changing the admitted 204-record semantic extension. No cumulative HTML artifact is claimed at this boundary.

Unit 7 adds the lecture/worksheet pair, four lecture segments, nineteen exercises, and exactly the three source-supplied solutions for Exercises 4, 7, and 13. All hint fields are blank, and the other sixteen exercises are explicit solution absences. It also adds five static/interactive asset identities, their component-rights records, twenty-nine artifacts, sixteen QA events, three correction records (including the TeX-safe display repair that leaves exact Unicode attribution intact), and 103 relations. Unit 7 adds 190 records, bringing the cumulative backend to 1,363; the validation receipt is `qa/unit-07/backend.json`. The complete 1,173-record Units 1–6 prefix is byte-identical. The manifest binds the final 117-page cumulative Unit 7 PDF, both downloadable GIF companion surfaces, and the deterministic build/structural/visual receipts. No cumulative HTML artifact is claimed at this boundary.

Unit 8 adds the lecture/worksheet pair, five section segments, twenty-one exercises, and exactly the two source-supplied solutions for Exercises 11 and 13. All hint fields are blank, the other nineteen exercises are explicit solution absences, and the media closure is empty. It adds nineteen artifacts, nine QA events, seven corrections (`O011-CORR-0073`–`0078` and `O011-TRANS-0079`), and 91 relations: 157 records in total.

Unit 9 adds the lecture/worksheet pair, three section segments, seventeen exercises, and no source-supplied solutions. All hint fields are blank and every solution absence is explicit. The Commons file `Tangentialvektor.svg` is bound with its public-domain rights record. Unit 9 adds twelve artifacts, seven QA events, eight corrections (`O011-CORR-0080`–`0086` and `O011-TRANS-0087`), and 69 relations: 121 records in total.

Unit 10 adds the lecture/worksheet pair, four section segments, thirty-one exercises, and exactly the four source-supplied solutions for Exercises 9, 10, 15, and 25. All hint fields are blank, and the other twenty-seven exercises are explicit solution absences. `Tangent bundle.svg` retains its public-domain status; `Torus vectors oblique.jpg` retains its CC BY-SA 3.0 component license and attribution. Before the cumulative reader closure, Unit 10 contributes twenty-eight artifacts, eleven QA events, sixteen corrections (`O011-CORR-0088`–`0101` and `O011-TRANS-0102`–`0103`), and 138 relations: 239 records in total. Binding the passing cumulative reader adds exactly the existing three HTML artifact records, one reader QA event, and four relations to Unit 10. The manifest, Unit 10 census, closure artifact metadata, and reader QA-event values additionally bind the companion PDF plus its structural and visual receipts without inflating the semantic record count.

Together Units 8–10 add 517 semantic records plus eight cumulative-reader closure records, bringing the bound cumulative backend to 1,888 records. The immutable Units 1–7 JSONL prefix is 812,882 bytes with SHA-256 `d9d51a46b84368f50a211a31263bafe8f1588f8e62a5fa4b496b2ff45903b912`; the corresponding 1,364-line CSV prefix is 288,506 bytes with SHA-256 `c301009770e1c523d046585c0c83947cab6f856b32b83890d361a919fed5a958`. The validation receipt is `qa/unit-10/backend.json`. The exporter binds the final readers only as one six-file closure: `output/html/unit-10/index.html`, its inventory manifest, the passing HTML receipt, `output/pdf/geometri-diferensial-manifold-mulus-hingga-unit-10-id.pdf`, `qa/unit-10/pdf_structural_qa.json`, and `qa/unit-10/PDF_VISUAL_QA.json` must all exist with the frozen exact bytes, and each receipt must bind its corresponding reader artifact. Any partial or stale closure is rejected.

Use an explicit checkpoint timestamp so a repeat export is byte-identical:

```text
python scripts/export_backend_v10.py --root . --checkpoint YYYY-MM-DDTHH:MM:SSZ --translation-state mathematically_reviewed
```

The generator rejects a changed Units 1–7 prefix; any drift from the frozen 525-record extension and 1,888-record combined census; stale Units 8–10 authority, translation, mathematical-QA, correction, or reader evidence; any exercise/point/hint/solution mismatch; incomplete component-rights evidence; schema violations; and unresolved stable-ID references. Use `--check-only` for a non-mutating full reconstruction. Run `python scripts/verify_backend_v10.py --root .` after export to verify canonical JSONL, the exact CSV projection, JSON Schema validity, reference closure, every source/target segment hash, exact solution-absence truth, live artifact hashes, component rights, every correction target and manifest, every QA receipt, the six-file HTML/PDF reader closure, model provenance, and a byte-identical two-cycle repeat export.
